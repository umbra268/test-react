{
  "scripts": {
    "pre:build": "rm -rf dist && mkdir dist",
    "build": "node processMarkdown.js > dist/pages.json && cp -r index.html app/ dist",
    "processMarkdown": "node processMarkdown.js > pages.json"
  },
  "devDependencies": {
    "front-matter": "^2.1.2",
    "highlight.js": "^9.9.0",
    "markdown-it": "^8.2.2",
    "markdown-it-abbr": "^1.0.4",
    "markdown-it-attrs": "^0.8.0",
    "markdown-it-deflist": "^2.0.1",
    "markdown-it-footnote": "^3.0.1",
    "markdown-it-sub": "^1.0.0"
  }
}
